services:
  rbac-db:
    image: postgres:16.1
    container_name: postgres-rbac-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: rbac
    ports:
      - "5434:5432"
